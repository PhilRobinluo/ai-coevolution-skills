# Mermaid 图表复核表

## 结构

- 主线是否一眼可见？
- 起点、终点、决策点是否容易区分？
- 是否存在没有入边或出边的孤立节点？
- 相同层级的节点是否使用相同语法？

## 语义

- 箭头表达顺序、调用、依赖还是归属？
- 条件分支是否包含明确标签？
- 节点名是否来自用户事实，而不是未经确认的推断？

## 技术验证

1. 保存为 UTF-8 `.mmd`。
2. 使用 `mmdc` 生成 SVG。
3. 确认退出码、SVG 文件存在且大小大于零。
4. 打开 SVG，抽查中文、箭头、换行和分组。
5. 在目标平台粘贴一次，确认平台支持所用图型。
